setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    web: frontend/Dockerfile
    api: backend/Dockerfile
  config:
    RAILS_ENV: development
#run:
#  web: PORT=3001 yarn --cwd frontend start
#  api: cd ./backend && PORT=3000 bundle exec rails server