setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    web: frontend/Dockerfile
    api: backend/Dockerfile
  config:
    RAILS_ENV: development
run:
  web: PORT=$PORT npm install -g serve && serve -s build
  api: bundle exec rails server -b 0.0.0.0